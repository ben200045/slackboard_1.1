<%= form_for(course) do |f| %>
  <% if course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(course.errors.count, "error") %> prohibited this course from being saved:</h2>

      <ul>
      <% course.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :course_name %>
    <%= f.text_field :course_name, autofocus: true %>
  </div>

  <div class="field">
    <%= f.label :course_description %>
    <%= f.text_field :course_description %>
  </div>

  <div class="field">
    <p></p></p>
    <%= f.label :course_start_date %>
    <%= f.text_field :course_start_date, class: "datepicker", placeholder: "YYYY/MM/DD" %>
  </div>

  <div class="field">
    <%= f.label :course_end_date %>
    <%= f.text_field :course_end_date, class: "datepicker", placeholder: "YYYY/MM/DD" %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
